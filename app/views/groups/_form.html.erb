<%= form_for(@group_page) do |f| %>
  <% if @group_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_page.errors.count, "error") %> prohibited this group_page from being saved:</h2>

      <ul>
      <% @group_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
